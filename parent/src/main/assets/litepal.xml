<?xml version="1.0" encoding="utf-8"?>
<litepal>
    <!--
        Define the database name of your application.
        By default each database name should be end with .db.
        If you didn't name your database end with .db,
        LitePal would plus the suffix automaticly for you.
        For example:
        <dbname value="demo" ></dbname>
    -->
    <dbname value="atdb"></dbname>
    <!--
       Define the version of your database. Each time you want
       to upgrade your database, the version tag would helps.
       Modify the models you defined in the mapping tag, and just
       make the version value plus one, the upgrade of database
       will be processed automaticly without concern.
           For example:
       <version value="57" ></version>
   -->
    <version value="60"></version>

    <list>
        <mapping class="com.tongxun.atongmu.parent.model.TokenIdModel"></mapping>
        <mapping class="com.tongxun.atongmu.parent.model.BabyInfoModel"></mapping>
        <mapping class="com.tongxun.atongmu.parent.model.ContactModel"></mapping>
    </list>
</litepal>
